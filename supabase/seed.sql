-- Optional: reference seed for SQL-first workflows.
-- The recommended path is `node scripts/seed-customers.mjs` with the service role key.
-- Alternatively run `tools/seed-demo-e46.ps1` or apply `supabase-seed-demo-e46.sql`
-- after creating the Auth user manually.

select 1;
